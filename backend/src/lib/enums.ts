/**
 * Enum for http status codes used for API error handling.
 * @readonly
 * @enum {number}
 */
export enum HttpStatusCode {
    OK = 200,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    INTERNAL_SERVER = 500,
   }